<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base platform theme (no Material Components dependency needed — Compose
         supplies the Material 3 colour scheme). Edge-to-edge is enabled in code. -->
    <style name="Theme.NationalGridLive" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/surface_light</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
